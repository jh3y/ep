doctype
head
  title progrecss workbench
  link(rel="stylesheet", href="css/progrecss.min.css")
  style.
    div {
      height: 40px;
    }
    body:after,
    div:after {
      animation-iteration-count        : infinite;
      -webkit-animation-iteration-count: infinite;
    }
body
  button#actionButton Play/Pause body progrecss
  h1 let's make progrecss
  .progrecss.blue(data-progrecss="75") blue
  .progrecss.mock.red(data-progrecss-mock="5") red mocked
  .progrecss.mock.staggered.green(data-progrecss-mock="5") staggered mock
  .progrecss.timer.purple(data-progrecss-timer="10") timer

  script(src="js/progrecss.js")
  script.
    (function(){
      var myprogrecss = new progrecss (document.body, {
        color: 'red',
        percent: 0,
        position: 'fixed',
        reverse: true,
        mock: {
            staggered: true,
            startMockOnCreate: true,
            mockDuration: 10
        }
      });
      document.getElementById('actionButton')
        .addEventListener('click', function(e) {
          myprogrecss.togglePause();
        });
    })();
